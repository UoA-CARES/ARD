# ARD (Autonomous RL Designer) — orchestrator dependencies.
# Training itself runs inside the per-job Isaac Lab docker image, so Isaac Lab /
# rl_games are NOT required here.
openai>=1.0           # LLM (OpenRouter-compatible) reward generation
PyYAML>=6.0           # config files
tensorboard>=2.11     # reading training TensorBoard summaries from artifacts
numpy>=1.23           # scalar summary statistics
tqdm>=4.64            # progress bars
opencv-python<5.0.0   # Slicing video frames for VLM feedback (cv2.VideoCapture)
