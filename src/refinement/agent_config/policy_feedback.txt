We trained a reinforcement learning (RL) policy using your `_get_rewards` method and tracked statistics over training. Below are the overall statistics and performance trends for every scalar logged, including each reward component and the task's fixed evaluation metric `fitness_function` (higher is better — this is what your reward is ultimately judged on, and you cannot change it).
