# Per-sample exploration directives appended to each reward candidate's prompt so
# the batch explores structurally different designs instead of i.i.d. rewordings.
# One directive per line. Blank lines and lines starting with '#' are ignored.
# An unsteered baseline ("") is prepended in code, so line 1 here maps to sample 1;
# directives cycle if there are more samples than directives.
Exploration directive: prioritize DENSE shaping — smooth, continuous feedback at every step that guides the policy toward the goal.
Exploration directive: try a SPARSE / terminal-heavy reward — large bonuses for reaching key milestones, with minimal step-wise shaping.
Exploration directive: add explicit PENALTY terms (energy, action magnitude, jerk, or constraint violations) and balance them against the task reward.
Exploration directive: decompose the reward into MORE components than usual, each isolating one distinct behavioral aspect.
Exploration directive: keep the reward MINIMAL — the fewest components that still capture the task goal.
Exploration directive: use NONLINEAR transforms (exp/tanh/log), each with its own temperature, to reshape and rebalance component magnitudes.
Exploration directive: emphasize a DIFFERENT objective trade-off than the obvious one (e.g. stability over speed, or precision over raw progress).
